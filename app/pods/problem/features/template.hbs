{{#ui-layout blueprint="problem-grid"}}
  {{#if (gt stage 0)}}
    {{#ui-section as |section|}}
      {{#section.h1}}
        Theming
      {{/section.h1}}
      {{section.divider}}
      {{#section.body}}
        Needs to play nice and blend in.
      {{/section.body}}
    {{/ui-section}}
  {{/if}}

  {{#if (gt stage 1)}}
    {{#ui-section as |section|}}
      {{#section.h1}}
        Components
      {{/section.h1}}
      {{section.divider}}
      {{#section.body}}
        You need a huuuuge library of components.
      {{/section.body}}
    {{/ui-section}}
  {{/if}}

  {{#if (gt stage 2)}}
    {{#ui-section as |section|}}
      {{#section.h1}}
        ARIA & I18N
      {{/section.h1}}
      {{section.divider}}
      {{#section.body}}
        Role, aria-*, focus handling, no text, etc.
      {{/section.body}}
    {{/ui-section}}
  {{/if}}

{{/ui-layout}}
