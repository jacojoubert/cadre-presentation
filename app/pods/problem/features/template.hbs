{{#ui-layout blueprint="problem-grid"}}
  {{#if (gt stage 0)}}
    {{#ui-section as |section|}}
      {{#section.h2}}
        Theming & Customization
      {{/section.h2}}
      {{section.divider}}
      {{#section.body}}
        Needs to play nice and blend into existing systems.
      {{/section.body}}
    {{/ui-section}}
  {{/if}}

  {{#if (gt stage 1)}}
    {{#ui-section as |section|}}
      {{#section.h2}}
        Component Selection
      {{/section.h2}}
      {{section.divider}}
      {{#section.body}}
        You need a huuuuge library of components.
      {{/section.body}}
    {{/ui-section}}
  {{/if}}

  {{#if (gt stage 2)}}
    {{#ui-section as |section|}}
      {{#section.h2}}
        ARIA & I18N
      {{/section.h2}}
      {{section.divider}}
      {{#section.body}}
        Role, aria-*, focus handling, no hard-coded text, etc.
      {{/section.body}}
    {{/ui-section}}
  {{/if}}

{{/ui-layout}}
